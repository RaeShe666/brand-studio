name: Release Windows Installer

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing tag to publish installer assets to (for release recovery)
        required: false
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
      VITE_API_URL: "https://api.sylailabs.com"
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.22.1"
          cache: npm

      - name: Install dependencies
        run: npm ci --include=optional

      - name: Check desktop authentication configuration
        shell: pwsh
        run: |
          if (-not $env:VITE_SUPABASE_URL -or -not $env:VITE_SUPABASE_ANON_KEY) {
            throw "Configure VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY repository secrets before publishing Brand Studio."
          }

      - name: Build Windows installer
        run: npm run build:win -- --publish never

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: brand-studio-windows-installer
          path: release/*/*-Setup.exe
          if-no-files-found: error

      - name: Publish GitHub Release asset
        if: startsWith(github.ref, 'refs/tags/') || inputs.release_tag != ''
        uses: softprops/action-gh-release@v2
        with:
          files: release/*/*-Setup.exe
          generate_release_notes: true
          tag_name: ${{ inputs.release_tag || github.ref_name }}
